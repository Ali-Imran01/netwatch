export default function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-gray-900">Dashboard</h1>
      <p className="mt-4 text-sm text-gray-500">
        Dashboard content (status board, latency charts, uptime %) arrives in Week 4. Use Inventory in the sidebar to manage sites, VLANs, subnets, IPs and devices.
      </p>
    </div>
  )
}
